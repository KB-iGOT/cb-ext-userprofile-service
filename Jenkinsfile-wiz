pipeline {
    agent any


    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Scan Repository') {
            steps {
                sh '''
                    # Scan the entire repository
                    wizcli dir scan .
                '''
            }
        }
    }
}